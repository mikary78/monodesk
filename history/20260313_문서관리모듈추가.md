# 변경 이력: 문서 관리 모듈 추가 (지결서·회의록)

## 기본 정보
- 날짜: 2026년 03월 13일
- 브랜치: master
- 작업자: Claude Code

## 요청 내용
법인 내부 문서(지결서, 회의록)를 관리하는 모듈 추가 요청.
동업자 4명 공동 운영 구조에서 지출 결의·회의 기록을 체계적으로 문서화하고 세무조사 대비 증빙 자료 확보.

## 변경한 내용

### 추가된 파일
- `backend/models/document.py` : Document SQLAlchemy 모델 (지결서·회의록 통합 테이블)
- `backend/schemas/document.py` : Pydantic V2 스키마 (Create/Update/Response)
- `backend/services/document_service.py` : CRUD 비즈니스 로직 + 문서번호 자동 채번
- `backend/routers/document.py` : FastAPI 라우터 (CRUD 5개 엔드포인트)
- `frontend/src/api/documentApi.js` : API 호출 함수 (fetchDocuments, createDocument 등)
- `frontend/src/pages/DocumentPage.jsx` : 문서 관리 페이지 (탭 구조)
- `frontend/src/components/modules/document/DocumentListTab.jsx` : 목록 테이블 컴포넌트
- `frontend/src/components/modules/document/DocumentFormModal.jsx` : 작성/수정 폼 모달
- `frontend/src/components/modules/document/DocumentDetailModal.jsx` : 상세 조회 모달
- `history/20260313_문서관리모듈추가.md` : 이 파일

### 수정된 파일
- `backend/main.py` : document 라우터 등록 추가
- `backend/database.py` : create_tables()에 models.document import 추가
- `frontend/src/App.jsx` : /documents 라우트 추가
- `frontend/src/components/layout/Sidebar.jsx` : "문서 관리" 메뉴 항목 추가 (FileText 아이콘)

### 데이터베이스 변경
- 신규 테이블: `documents`
  - 공통 필드: doc_number, doc_type, title, author, doc_date, status, content, tags
  - 지결서 전용: expense_amount, expense_category, expense_vendor, payment_method
  - 회의록 전용: meeting_location, attendees, agenda, decisions
  - 문서번호 자동 채번: 지결-YYYY-NNN / 회의-YYYY-NNN 형식

## 테스트 및 검증 방법
- [x] `/api/documents` GET 응답 확인 (빈 배열 [])
- [ ] 지결서 작성 → 저장 → 목록 조회
- [ ] 회의록 작성 → 저장 → 목록 조회
- [ ] 확정 상태 전환 → 수정 버튼 잠금 확인
- [ ] 삭제 → ConfirmDialog 동작 확인
- [ ] 문서번호 자동 채번 확인 (지결-2026-001)

## 주의사항
- 확정(확정) 상태의 문서는 UI에서 수정 버튼이 비활성화됨 (법적 문서 무결성 보호)
- 문서 삭제는 소프트 삭제 (is_deleted=1), 물리적 삭제 아님

## 향후 작업 제안
1. 기능 4: 주주총회 의사록 (corporate 모듈 확장)
2. 기능 1+2 통합: 매입 영수증 OCR → 지출+재고 동시 반영 (ocr_service.py 이미 생성됨)
3. 문서 PDF 출력 (weasyprint 또는 브라우저 @print CSS)
4. 문서 템플릿 기능 (자주 쓰는 양식 저장)
