# MonoDesk Backend - Python 의존성 목록
# 설치: pip install -r requirements.txt

# --- 코어 프레임워크 ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- 데이터베이스 ---
sqlalchemy>=2.0.27

# --- 데이터 검증 ---
pydantic>=2.9.0

# --- CORS 미들웨어 ---
python-multipart>=0.0.9

# --- 인증 (JWT + bcrypt) ---
python-jose[cryptography]>=3.3.0   # JWT 토큰 생성/검증
passlib[bcrypt]>=1.7.4             # bcrypt 비밀번호 해시
bcrypt==4.0.1                      # passlib과의 호환성 고정 (5.x 비호환)

# --- 환경변수 ---
python-dotenv>=1.0.0

# --- 테스트 ---
pytest>=8.0.0
httpx>=0.27.0

# --- AI/OCR ---
anthropic>=0.40.0       # Claude Vision API (영수증 OCR)

# [비활성] Tesseract 기반 OCR — Claude Vision API로 교체됨 (2026-04-07)
# pytesseract>=0.3.10   # [비활성] Tesseract OCR → Claude Vision API로 교체
# Pillow>=10.0.0        # [비활성] pytesseract 의존성
# opencv-python>=4.8.0  # [비활성] 이미지 전처리 (Claude Vision API 불필요)

# --- Phase 2 예정 (AI 기능) ---
# ollama  (Ollama Python SDK)
# faster-whisper  (음성 인식)
# chromadb  (벡터 DB)
# langchain  (AI 오케스트레이션)
